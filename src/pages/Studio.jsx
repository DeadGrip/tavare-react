export default function Studio() {
  return (
    <div className="wrap">
      <div className="section-head">
        <div className="eyebrow">Studio</div>
        <h2>Coming in the next build pass</h2>
        <p style={{ marginTop: 14 }}>
          The admin panel — login, design uploads, and the reservations table — is being rebuilt for
          React next. Until then, keep using the previous version of Studio to manage the catalogue.
        </p>
      </div>
    </div>
  );
}
